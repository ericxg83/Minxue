# 敏学项目开发规范

## 项目定位

敏学是一套面向学生和教师的智能学习系统，核心能力包括：

- 作业图片上传、OCR 识别和 AI 批改
- 错题沉淀、错题重练和学习效果追踪
- 练习册、答案库、题目资源和组卷
- 知识点诊断与学生掌握度分析
- 讲义、讲课脚本和变式题生成

项目包含：

- React 移动端：`src/`
- Vue 教师工作台：`src/workbench/`
- Express 后端：`server/`
- BullMQ/Redis 异步任务：`server/worker.js`、`server/queue.js`
- PostgreSQL/Neon 数据库：`database/`
- OSS 文件存储和 AI/OCR 服务

详细业务流程见：

- `docs/architecture.md`
- `docs/grading-flow.md`
- `docs/wrong-question-system.md`
- `docs/knowledge-system.md`
- `docs/handout-system.md`

## 核心业务原则

1. 批改结果不仅用于展示，还会影响题目记录、错题记录、知识点关联和掌握度。
2. 错题是长期学习数据，不是一次批改产生的临时结果。
3. 普通题目错题和练习册自包含错题属于不同数据场景，不能简单合并处理。
4. 答案库中的 AI 草稿、教师审核答案和官方答案具有不同可信级别。
5. 题目与知识点是多对多关系，学生掌握度属于学生维度的数据。
6. 知识点关联和掌握度更新可能异步执行，系统需要接受最终一致性。
7. 讲义、题目和答案等 JSONB 数据需要考虑历史数据兼容。
8. 优先复用现有服务、组件、队列、缓存和数据模型，避免重复开发。
9. 错题「同一题」判定统一走 `src/domain/questionIdentity.js`，按归一化后的身份键精确匹配，禁止用相似度阈值自动合并错题。
10. 当前产品口径是「只练错题」：重练与组卷只使用学生真实做错的题目，变式题（`variant_questions`）仅作讲义素材，不进入重练卷。

## 架构关键提醒

1. 后端同时存在历史内联 API 和 `server/routes/` 拆分后的新 API，修改前必须确认真实调用入口。
2. `server/worker.js` 承担批改主流程，修改批改相关逻辑时必须检查同步和异步执行路径。
3. 数据库静态 Schema 与迁移链可能存在差异，涉及数据库时应以实际迁移和运行结构为准。
4. `tasks`、`questions`、`wrong_questions` 等核心表经历过多次演进，不能随意恢复级联删除或改变外键行为。
5. `wrong_questions.question_id` 可能为空，用于支持练习册自包含错题。
6. 普通错题和练习册错题使用不同的去重和定位方式。
7. 资源答案定位不能只依赖题号，必须关注单元、章节、题号和小题号。
8. `judgements` 是判题审计数据，人工复核、AI 判题和重判结果需要保留正确来源语义。
9. 移动端 React 和 PC Vue 可能共同依赖同一套 API，公共接口修改必须同时检查两端。
10. 修改数据写入逻辑时必须检查前端缓存失效和异步同步行为。

## 修改代码纪律

1. 修改前先阅读目标文件、调用方、相关 API、数据库表和迁移。
2. 先分析影响范围，再决定实现方式。
3. 优先使用已有能力，不重复实现已有服务或数据逻辑。
4. 保持修改范围最小，不进行无关重构、格式化或目录调整。
5. 修改公共 API 时，同时检查移动端、PC 端、Worker、缓存和历史调用。
6. 修改数据库、状态机、JSONB 结构或核心业务流程时，必须考虑旧数据兼容。
7. 核心流程修改后，至少验证成功路径、异常路径和历史数据路径。
8. 保留工作区中已有的用户修改，不擅自回滚或覆盖。
9. 完成后说明修改内容、影响范围、验证结果和遗留风险。

## 禁止事项

1. 未经确认，不得直接修改数据库 Schema、迁移、索引、约束或核心数据结构。
2. 未经确认，不得直接修改作业批改、错题生命周期、重练、组卷或掌握度计算流程。
3. 不得删除或绕过答案审核状态、判题审计记录或历史兼容逻辑。
4. 不得把练习册答案仅按题号匹配。
5. 不得把变式题或其他 AI 生成题混入错题重练卷和组卷题目。
6. 不得引入基于相似度阈值的错题自动合并逻辑。
7. 不得随意删除错题、任务、题目或用户数据。
8. 不得假设旧 API 与新路由完全等价。
9. 不得在没有影响分析的情况下修改共享服务、公共状态或任务状态。
10. 涉及数据库或核心流程的修改，必须先说明方案、影响范围、兼容策略和验证方式，等待负责人确认后再实施。
