<template><header class="ds-page-header"><div><div v-if="eyebrow" class="eyebrow">{{ eyebrow }}</div><div class="title-row"><h1>{{ title }}</h1><slot name="badge" /></div><p v-if="description">{{ description }}</p></div><div v-if="$slots.actions" class="actions"><slot name="actions" /></div></header></template>
<script setup>defineProps({ title: { type: String, required: true }, description: String, eyebrow: String })</script>
<style scoped>.ds-page-header{display:flex;align-items:flex-end;justify-content:space-between;gap:24px;margin-bottom:24px}.eyebrow{margin-bottom:7px;color:var(--wb-primary);font-size:11px;font-weight:650;letter-spacing:.05em}.title-row{display:flex;align-items:center;gap:10px}.ds-page-header h1{margin:0;color:var(--wb-text);font-size:26px;font-weight:680;line-height:1.25;letter-spacing:-.025em}.ds-page-header p{margin:7px 0 0;color:var(--wb-text-secondary);font-size:13px}.actions{display:flex;align-items:center;justify-content:flex-end;gap:8px;flex-wrap:wrap}@media(max-width:720px){.ds-page-header{align-items:flex-start;flex-direction:column}.actions{justify-content:flex-start}}</style>
