fix(讲义): 修复品牌名"明学"→"敏学" + AI讲解防回显prompt

1. 品牌名修正（3处）：
   - HandoutPreview.vue 封面标签
   - handoutDocxService.js docx元数据
   - docxGenerator.js 通用docx生成器

2. AI讲解防回显（核心修复）：
   - 新增 isPromptEcho() 检测AI是否回显了prompt指令文本
   - 新增 buildFallbackExplanation() 生成结构化兜底文档
   - generateKnowledgeExplanation 改为2次重试+兜底模板
   - generateEnglishExplanation 同样增加防回显+兜底

3. 讲解引导升级：
   - buildLectureGuidance 从简单提示词改为结构化教学引导
   - 包含诊断、引导步骤、提问示例、验证环节
   - 英文模板同步升级